{
  "stepTimeMs" : 5000,
  "timeOffset" : 1493837313104,
  "sources" : [
    {
    "inputFile" : "/root/lanl/data/auth.txt",
    "outputTopic" : "auth",
    "filter" : "source_user == 'U66@DOM1' || (ip_src_addr not in [ null, 'TGT', '?'] && (not(exists(hostname_filter)) || ip_src_addr in hostname_filter) ) || (ip_dst_addr not in [ null, 'TGT', '?' ] && (not(exists(hostname_filter)) || ip_dst_addr in hostname_filter))",
    "config" : {
     	"columns" : [ "timestamp"
                    , "source_user"
                    , "dest_user"
                    , "ip_src_addr"
                    , "ip_dst_addr"
                    , "auth_type"
                    , "logon_type"
                    , "auth_orientation"
                    , "success"
                    ]
               }
    }
  ]
}
